randomNumber[0] = randomnizer(1, 10);
                for (i = 1; i < randomNumber.size; i++)
                {
                    randomNumber[i] = randomnizer(0, 10);
                    while(randomNumber[i] == randomNumber[i - 1])
                    {
                        randomNumber[i] = randomnizer(0, 10);






                if (DifficultyChecker()=="Easy")
                {
                    while (randNumber.Length != EasyDigits)
                    {
                        randNumber = randomNumber();
                    }
                }
                else if (DifficultyChecker() == "Medium")
                {
                    while (randNumber.Length != MediumDigits)
                    {
                        randNumber = randomNumber();
                    }
                }
                else if (DifficultyChecker() == "Hard")
                {
                    while (randNumber.Length != HardDigits)
                    {
                        randNumber = randomNumber();
                    }
                }
                else if (DifficultyChecker() == "Insane")
                {
                    while (randNumber.Length != InsaneDigits)
                    {
                        randNumber = randomNumber();
                    }
                } 






            /*
             * 
            bool[] chosenNumbers = new bool[10];
            for (int i = 0; i < 10; i++) {
                chosenNumbers[i] = false;
            }

            String randNumber = "";
            bool found = false;
            int i = randomGen (5);
            if (!bool[i]) {
                bool[i] = true;
                randNumber += i.ToString;
            } else {
                while (!found) {
                    i++;
                    if (!bool[i]) {
                        found = true;
            */





                /*numberToPick = randomNum.Next(0, 10);
                if (!OnetoNine[numberToPick])
                {
                    OnetoNine[numberToPick] = true;
                    randNumber += Convert.ToString(numberToPick);
                }
                else
                {
                    while (!found)
                    {
                        numberToPick = randomNum.Next(0, 10);
                        if (!OnetoNine[numberToPick])
                        {
                            randNumber += Convert.ToString(numberToPick);
                            found = true;
                        }
                    }
                }*/

